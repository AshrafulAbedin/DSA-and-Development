#include <bits/stdc++.h>
using namespace std;
class Node{
    int data;
    Node* next;
    Node(int val)
    {
        data = val;
        next = nullptr;
    }
};

int main(void)
{
    
    return 0;
}